{
  "name": "@workspace/publika",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --config vite.config.ts --host 0.0.0.0",
    "build": "vite build --config vite.config.ts",
    "serve": "vite preview --config vite.config.ts --host 0.0.0.0",
    "typecheck": "echo 'No TypeScript to check (vanilla JS)'",
    "postinstall": "chmod -R +x node_modules/.bin"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "vite": "^7.3.3"
  },
  "dependencies": {
    "axios": "^1.16.0"
  }
}
